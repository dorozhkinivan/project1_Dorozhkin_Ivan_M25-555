# Лабиринт сокровищ

**Лабиринт сокровищ** — это консольная текстовая adventure-игра на Python. Вы выступаете в роли отважного исследователя, проникшего в древний лабиринт, полный загадок, ловушек и скрытых артефактов.

Ваша цель — найти ключи, решить головоломки древних мудрецов и добраться до сокровищницы, чтобы забрать главный приз. Но будьте осторожны: один неверный шаг может активировать ловушку!

---

## Демонстрация игрового процесса

Ниже представлена демонстрация работы программы.

[![asciicast](https://asciinema.org/a/NnxjWqlZNQ9JvPQw.svg)](https://asciinema.org/a/NnxjWqlZNQ9JvPQw)

---

## Установка и запуск

Для запуска проекта вам потребуется **Python 3.14+** и пакетный менеджер **Poetry**.

### 1. Клонирование репозитория

```bash
git clone https://github.com/dorozhkinivan/project1_Dorozhkin_Ivan_M25-555.git
cd project1_Dorozhkin_Ivan_M25-555
```

### 2. Установка зависимостей

Вы можете воспользоваться `Makefile` для быстрой установки:

```bash
make install
```

Или использовать команды `poetry` напрямую:

```bash
poetry install
```

### 3. Запуск игры

Для старта игры выполните:

```bash
make project
```

Альтернативный запуск через poetry:

```bash
poetry run project
```

---

## 🎮 Управление

Игра управляется текстовыми командами в консоли.

| Команда | Описание | Примеры |
|---------|----------|---------|
| `go <направление>` | Перемещение между комнатами | `go north` (или просто `north`), `go east` |
| `look` | Осмотреться в текущей комнате | `look` |
| `take <предмет>` | Поднять предмет | `take sword`, `take key` |
| `use <предмет>` | Использовать предмет из инвентаря | `use rusty_key` |
| `inventory` | Показать содержимое рюкзака | `inventory` |
| `solve` | Решить загадку / Открыть сундук | `solve` |
| `help` | Показать список команд | `help` |
| `quit` | Выйти из игры | `quit` |

---

## О проекте и технологиях

Проект разработан в учебных целях для демонстрации работы с Python и Git.

**Особенности разработки:**
- **Чистый Python**: Никаких сторонних библиотек для логики (только стандартный модуль `math`).
- **Процедурный стиль**: Отказ от классов (ООП) в пользу функций и чистого разделения данных и логики.
- **Архитектура**:
  - `main.py`: Точка входа и игровой цикл.
  - `constants.py`: Данные (карта комнат, тексты).
  - `player_actions.py`: Логика игрока (движение, инвентарь).
  - `utils.py`: Вспомогательные функции (загадки, ловушки, рандом).
- **Качество кода**: Код соответствует стандарту **PEP8**. Для проверки используется линтер **Ruff**.

### Проверка качества кода

Чтобы проверить проект линтером, выполните:

```bash
make lint
```

---

## Автор

**Иван Дорожкин**